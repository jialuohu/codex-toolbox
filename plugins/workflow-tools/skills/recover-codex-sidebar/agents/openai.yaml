interface:
  display_name: "Recover Codex Sidebar"
  short_description: "Restore project mappings after a Codex account switch."
  default_prompt: "Use $recover-codex-sidebar to inspect and restore my missing sidebar project mappings after switching Codex accounts."
policy:
  allow_implicit_invocation: true

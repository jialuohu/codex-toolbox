interface:
  display_name: "Sync Toolbox"
  short_description: "Apply and verify the published toolbox on this machine."
  default_prompt: "Use $sync-toolbox to update this machine from published main and verify the full managed setup."
policy:
  allow_implicit_invocation: true

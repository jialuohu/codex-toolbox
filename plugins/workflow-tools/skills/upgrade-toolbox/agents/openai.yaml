interface:
  display_name: "Upgrade Toolbox"
  short_description: "Audit upstreams and upgrade compatible toolbox components."
  default_prompt: "Use $upgrade-toolbox to check upstream updates and report compatible candidates, deferred migrations, and coverage gaps."
policy:
  allow_implicit_invocation: true

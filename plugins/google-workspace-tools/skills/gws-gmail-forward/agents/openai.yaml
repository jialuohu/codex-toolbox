interface:
  display_name: "Draft or Forward Gmail"
  short_description: "Prepare a Gmail forward from an isolated Gmail profile."
  default_prompt: "Use $gws-gmail-forward with an explicit alias to prepare a draft."
policy:
  allow_implicit_invocation: true

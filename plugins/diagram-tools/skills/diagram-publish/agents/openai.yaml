interface:
  display_name: "Diagram Publish"
  short_description: "Publish reviewed diagrams to Cloudflare Pages"
  default_prompt: "Use $diagram-publish to inspect sharing setup or publish accepted HTML after visual review and installation-specific opt-in."
policy:
  allow_implicit_invocation: true

interface:
  display_name: "Stevens Slides — White"
  short_description: "Create Stevens slides in the White theme"
  default_prompt: "Use $stevens-slides-white to create a Stevens presentation in the White theme."
policy:
  allow_implicit_invocation: true

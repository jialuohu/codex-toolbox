interface:
  display_name: "WeChat Reader & Digest"
  short_description: "Read current articles or digest selected WeChat sources"
  default_prompt: "Use $wechat-digest for interactive reading of current configured WeChat articles or a safe incremental digest."

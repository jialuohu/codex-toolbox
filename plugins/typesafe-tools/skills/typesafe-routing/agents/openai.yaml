interface:
  display_name: "TypeSafe Capability Routing"
  short_description: "Rank optional skills and tools for eligible tasks"
  default_prompt: "Use $typesafe-routing by default for eligible tasks with a material capability choice; private content requires private_data_enabled: true in fresh status."
policy:
  allow_implicit_invocation: true

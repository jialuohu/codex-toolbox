# docmost-tools server

This package currently contains configuration and stable result contracts only.
Network access, browser authentication, and MCP tool registration are deliberately out of scope.

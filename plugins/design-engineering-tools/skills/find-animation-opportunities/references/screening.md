# Screening criteria

Good candidates include an occasional dialog entering from its trigger, a rare
success acknowledgement, or a jarring list-state change that needs spatial
continuity. Use `transform` and `opacity`, responsive easing, and short UI
durations. Keep hover motion behind fine-pointer media queries and provide a
reduced-motion alternative. Report why an opportunity is worth its frequency
cost, not just how to implement it.

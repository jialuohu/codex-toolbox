# Picker requirements

Render one full-size variant at a time in realistic context. Label each with
its named direction and axis; make switching instant and keyboard-accessible.
Before handoff, confirm every variant is reachable, interactions work, and the
console is clean. Present the variant, its tradeoff, and when it fits; do not
choose on the user's behalf.

interface:
  display_name: "PaperRead Draft"
  short_description: "Create a compact PaperRead note shell."
  default_prompt: "Use $paper-read-draft to prepare a brief Obsidian PaperRead draft for this paper and return a clickable Obsidian link."
policy:
  allow_implicit_invocation: true
